# Set extension policy
Configure forced install in Google Admin and set managed storage keys:
collectorUrl, keyId, sharedSecret, batchSize, flushIntervalMs, debug, collectTitles, inactivityTimeoutMinutes.
